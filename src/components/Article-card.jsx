import React from 'react';

function Article-card({article}) {
    return (
        <div>
            {article.title}
            
        </div>
    );


export default Article-card(props) {
    return (
        <div>
            
        </div>
    );
;